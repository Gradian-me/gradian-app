# Use internal npm registry (installs/publish). For security audit use: npm run audit (uses registry.npmjs.org)
registry=https://reg.cinnagen.com/repository/npm-group/

# Suppress peer dependency warnings for packages that haven't updated their peer deps
# The overrides in package.json handle React 19 compatibility
legacy-peer-deps=false

# Network timeout settings
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000

