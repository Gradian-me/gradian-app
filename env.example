# 2FA Service Credentials
NEXT_PUBLIC_CLIENT_ID=""
NEXT_PUBLIC_SECRET_KEY=""
CLIENT_ID=""
SECRET_KEY=""

# Backend URLs
URL_SCHEMA_CRUD=""
URL_DATA_CRUD=""
URL_SYNC_MAIL_TEMPLATE=""
NEXT_PUBLIC_URL_SCHEMA_CRUD=""
NEXT_PUBLIC_URL_DATA_CRUD=""
NEXT_PUBLIC_URL_SYNC_MAIL_TEMPLATE=""
URL_SEND_EMAIL=""



# SHA1 Key for encrypt decrypt
NEXT_PUBLIC_ENCRYPTION_KEY=""


# Next.js
NEXTAUTH_SECRET=""
NEXTAUTH_URL=""

# Internal API Configuration (for server-side requests) - PRIMARY SOURCE
# This is the PRIMARY source for internal API base URL when making server-side calls.
# If set, this will be used instead of DNS-based extraction from request headers.
# Priority: INTERNAL_API_BASE_URL > DNS extraction from headers > VERCEL_URL > localhost
# If not set, the system will fall back to extracting from request headers (x-forwarded-host or host header)
# for multi-tenant deployments, or localhost for development.
INTERNAL_API_BASE_URL=""

# Development Tenant Domain (for local development) - OPTIONAL
# When accessing the app via localhost in development mode, tenant domain cannot
# be extracted from DNS. Set this to specify a default tenant for local development.
# Example: DEV_TENANT_DOMAIN=dev (to match one of your backend tenant identifiers)
DEV_TENANT_DOMAIN=""

# Note: Both tenant domain and internal API base URL are automatically extracted
# from DNS/hostname (x-forwarded-host or host header) for multi-tenant deployments.
# Tenant domain comes from the browser/app domain the user is accessing, NOT from
# backend service URLs. No environment variable configuration needed for normal operation.

# External Authentication Service
APP_ID=""
URL_AUTHENTICATION=""

# JWT Authentication Configuration
# JWT_SECRET is used for signing and verifying JWT tokens
# If not set, will fallback to NEXTAUTH_SECRET
JWT_SECRET=""
# Access token expiration time in seconds (default: 3600 = 1 hour)
JWT_ACCESS_TOKEN_EXPIRY=3600
# Refresh token expiration time in seconds (default: 604800 = 7 days)
JWT_REFRESH_TOKEN_EXPIRY=604800

# Password Security Configuration
# PEPPER is a secret value added to passwords before hashing with Argon2        
# This provides an additional layer of security even if the database is compromised                                                                             
# Generate a strong random string (e.g., using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")                                       
# IMPORTANT: Generate your own unique PEPPER value and keep it secret!
# Example (replace with your own generated value):
PEPPER=""

# Schema API Configuration
# Override the schema API base path (default: /api/schemas)
# Do not use quotes - Next.js will include them as part of the value
NEXT_PUBLIC_SCHEMA_API_BASE=/api/schemas

AVALAI_API_KEY=

